{# Basic Component #}


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Religius, Disiplin, Kreatif, Inovatif dan Mandiri.">
    <meta name="keywords" content="smkn 1 kemang, smk negeri 1 kemang, smk 1 kemang, smkn satu kemang, smk negeri satu kemang" />

    {# Favicon #}
    <link rel="icon" href="/assets/favicon.ico" type="image/x-icon"/>
    
    {# Google font #}
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;1,100;1,200;1,300;1,400;1,500;1,600;1,700&display=swap" rel="stylesheet">
    
    {# Bootstrap #}
    <link rel="stylesheet" href="/library/bootstrap-v5/css/bootstrap.min.css">

    {# Additional style digunakan untuk menambahkan style yang dibutuhkan di setiap halaman #}
    {{ additionalStyle | safe }}

    {# CSS #}
    <link rel="stylesheet" href="/styles/index.css">

    <!-- Global site tag (gtag.js) - Google Analytics -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=UA-205587124-1"></script>
    <script>
        window.dataLayer = window.dataLayer || [];
        function gtag(){dataLayer.push(arguments);}
        gtag('js', new Date());

        gtag('config', 'UA-205587124-1');
    </script>
    <!-- Google Analytics -->
        <script>
        (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
        (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
        m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
        })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');

        ga('create', {
            trackingId: 'UA-XXXXX-Y',
            cookieDomain: 'auto'
        });
            ga('send', {
            hitType: 'pageview'
        });

        ga(function(tracker) {
            // Logs the trackers name.
            // (Note: default trackers are given the name "t0")
            console.log(tracker.get('name'));

            // Logs the client ID for the current user.
            console.log(tracker.get('clientId'));

            // Logs the URL of the referring site (if available).
            console.log(tracker.get('referrer'));
        });
    </script>
    <!-- End Google Analytics -->

    <title>{{ title }}</title>
</head>
<body>
    {# Navbar component #}
    {% include 'navbar.njk' %}

    {# Content component #}
    {{content | safe}}
    
    {# Footer component #}
    {% include 'footer.njk' %}

    {# JQuery #}
    <script src="/library/jquery/jquery.js"></script>
    
    {# Bootstrap #}
    <script src="/library/bootstrap-v5/js/bootstrap.min.js"></script>
    
    {# Additional script digunakan untuk menambahkan script yang dibutuhkan di setiap halaman #}
    {{ additionalScript | safe }}
</body>
</html>